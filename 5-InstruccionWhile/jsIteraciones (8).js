function mostrar()
{

	var contador;
	var positivo;
	var negativo;

	contador=0;
	positivo=0;
	negativo=1;

	
	var respuesta='si';
	var contadorAlumnos=0;
	var edad;
	//var queresContinuar= "si";

	//while(queresContinuar!= "no")
	while(contadorAlumnos<)
	{
		edad=prompt("Ingrese edad");
		edad=parseInt(edad);
		if (edad==66) 
		{

			break;
		}
		if (contadorAlumnos==100) 
		{
			break;
		}
		contadorAlumnos=contadorAlumnos+1; //contadorDeVueltas=++
		//queresContinuar=prompt("para salir ingrese no");
	}

	if (contadorAlumnos==100) 
	{
		
	}


document.getElementById('suma').value=positivo;
document.getElementById('producto').value=negativo;

}//FIN DE LA FUNCIÓN